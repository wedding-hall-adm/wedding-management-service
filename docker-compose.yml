version: '2'
services:
  db:
    image: mysql:latest
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: wedding_halls
      MYSQL_USER: user
      MYSQL_PASSWORD: user